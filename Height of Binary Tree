//C++

M-1 (1 + max(left,right))

int height(struct Node* root){
        // code here 
        //base condition
        if(root==NULL)
        return 0;
        
        return(1+ max(height(root->left),height(root->right)));
        
    }
